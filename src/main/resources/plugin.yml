name: RestoreChunk
version: '${version}'
main: dev.warriorrr.restorechunk.RestoreChunk
api-version: '${minecraft_version}'
load: STARTUP
authors: [ Warriorrr ]
folia-supported: true

commands:
  restorechunk:
    usage: "Restores a chunk to its previous state."
    permission: restorechunk.command.restorechunk

permissions:
  restorechunk.command.*:
    default: op
    children:
      restorechunk.command.restorechunk: true
